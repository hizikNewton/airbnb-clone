import React from 'react'
import { NavContainer } from './styled'

type Props = {}

const Nav = (props: Props) => {
    return (
        <NavContainer>NavOptions</NavContainer>
    )
}

export default Nav